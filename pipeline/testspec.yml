#
# testspec.yml
#

version: 0.2

#env:
#  variables:
#    config_file: cmd/tests/service_test.yml
#    GOROOT: 
#    GOPATH:

phases:
  install:
    commands:
      - echo TEST_ENDPOINT = [$TEST_ENDPOINT]
#      - go get github.com/parnurzeal/gorequest
#  pre_build:
#    commands:
#      - printenv
#      - echo endpoint: $TEST_ENDPOINT > $config_file
  build:
    commands:
      - echo test sequence begins at `date`
      - GOROOT=''; GOPATH=''; make test
  post_build:
    commands:
      - echo test sequence complete `date`

#
# end of file
#
